function window$getter(){
  return _NativeDomGlobalProperties$Dart.getWindow$member();
}

function window$named($n, $o){
  var seen = 0;
  var def = 0;
  if (seen != $o.count || seen + def + $n != 0)
    $nsme();
  return window$getter();
}

function dom_window$getter(){
  return _NativeDomGlobalProperties$Dart.getWindow$member();
}

function dom_window$named($n, $o){
  var seen = 0;
  var def = 0;
  if (seen != $o.count || seen + def + $n != 0)
    $nsme();
  return dom_window$getter();
}

function document$getter(){
  return _NativeDomGlobalProperties$Dart.getDocument$member();
}

function document$named($n, $o){
  var seen = 0;
  var def = 0;
  if (seen != $o.count || seen + def + $n != 0)
    $nsme();
  return document$getter();
}

